<h1>New page</h1>

--echoing tags --

<%= form_for @page do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :author %>
  <%= f.text_field :author%>

  <%= f.label :body %>
  <%= f.text_area :body%>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit %>
<% end %>